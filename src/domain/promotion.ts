export type PromotionStatus = 'draft' | 'approved' | 'scheduled' | 'active' | 'archived'
export type PromotionLayout = 'auto' | 'single' | 'double' | 'triple' | 'hero' | 'qr'
export interface PromotionProduct { id?: string; name: string; short_description: string; original_price: number | null; promotional_price: number; image_key: string | null; image_url: string | null; badge_text: string; note: string; position: number; image_transform: { scale: number; x: number; y: number } }
export interface PromotionDesign { backgroundColor: string; textColor: string; priceColor: string; highlightColor: string; gradient: boolean; additionalText: string; qrCodeUrl: string; instagram: string; whatsapp: string; address: string; note: string; fontFamily: string; alignment: 'left' | 'center'; borderRadius: number; shadow: boolean; sealText: string; resolution: '1920x1080' | '1280x720' }
export interface PromotionRecord { id: string; company_id: string; title: string; subtitle: string | null; status: PromotionStatus; layout_type: PromotionLayout; design: PromotionDesign; display_ids: string[]; duration_seconds: number; schedule: Record<string, unknown>; current_media_id: string | null; approved_at: string | null; created_at: string; updated_at: string; products?: PromotionProduct[] }
export const defaultPromotionDesign: PromotionDesign = { backgroundColor: '#123d2c', textColor: '#ffffff', priceColor: '#ffd45c', highlightColor: '#e5484d', gradient: true, additionalText: '', qrCodeUrl: '', instagram: '', whatsapp: '', address: '', note: '', fontFamily: 'Arial', alignment: 'center', borderRadius: 28, shadow: true, sealText: 'OFERTA', resolution: '1920x1080' }
export const blankPromotionProduct = (position = 0): PromotionProduct => ({ name: '', short_description: '', original_price: null, promotional_price: 0, image_key: null, image_url: null, badge_text: '', note: '', position, image_transform: { scale: 1, x: 0, y: 0 } })
